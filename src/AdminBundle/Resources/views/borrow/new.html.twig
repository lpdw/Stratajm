{% extends "AdminBundle::admin-layout.html.twig" %}

{% block content %}
    <h1>Déclarer un emprunt</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <a href="{{ path('admin_borrow_index') }}"><button type="button" class="btn btn-primary">Retour à la liste</button></a>
        <button type="submit" class="btn btn-success pull-right">Créer</button>
    {{ form_end(form) }}
{% endblock %}
